name: CI

on: 
  push:
    branches: -master

jobs:
  build:

    runs-on: windows-latest

    steps:
    - uses: actions/checkout@v2
    - name: Show PATH
      run: echo ${env:PATH}
    - name: Build
      run: .\SelfTestBuild.ps1
      shell: powershell
